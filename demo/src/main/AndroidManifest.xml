<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.usb.host" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:name=".MultiCameraNewActivity"
            android:exported="false" />
        <activity
            android:name=".SetFrameCallbackActivity"
            android:exported="false" />
        <activity
            android:name=".RecordVideoActivity"
            android:exported="false" />
        <activity
            android:name=".TakePictureActivity"
            android:exported="false" />
        <activity
            android:name=".MultiCameraActivity"
            android:exported="false" />
        <activity
            android:name=".MultiPreviewActivity"
            android:exported="false" />
        <activity
            android:name=".CustomPreviewActivity"
            android:exported="false" />
        <activity
            android:name=".BasicPreviewActivity"
            android:exported="false" />
        <activity
            android:name=".EntryActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </activity>
    </application>

</manifest>
